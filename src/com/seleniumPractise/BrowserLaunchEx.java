package com.seleniumPractise;

public class BrowserLaunchEx {
	/*
	 * Selenium: ============ This is an Automation tool.
	 * 
	 * This has 4 components: ============================
	 * 
	 * 1. Selenium IDE(Integrated Development Environment) It is used only for
	 * firefox It can be used only for simple test cases.
	 * 
	 * 2. Selenium RC(Remote control)1.0 version Installation will be complicated by
	 * the server
	 * 
	 * 3. Selenium WebDriver--> Automation Framework Tool It supports all
	 * programming language for developing test script It is programattic an
	 * interface It can be easily integrated with eclipse
	 * 
	 * 4. Selenium Grid It is used for exceuting test cases across different system
	 * or machines We can perform parallel execution so that the execution time can
	 * be reduced. Used for Hub- node concepts We can run in one machine (Hub)&
	 * execute in different machines.
	 * 
	 * 
	 * 
	 * RC + Webdriver-->2.0(advanced version 3.0)
	 * 
	 * selenium server-standalone 3.141.59
	 * 
	 * WebDriver: ============ Its a collection of open source API (Application
	 * programming Interface) These are used to automate the web applications It
	 * supports many browsers like Chrome,IE,Firefox,Safari etc...
	 * 
	 * 
	 * 
	 * Driver name .exe Classname
	 * 
	 * Chrome webdriver.chrome.driver chromedriver.exe ChromeDriver()
	 * InternetExplorer webdriver.ie.driver IEDriverServer.exe
	 * InternetExplorerDriver() Firefox webdriver.gecko.driver geckodriver.exe
	 * FirefoxDriver();
	 * 
	 * 
	 * Driver methods: ================= driver.navigate().to()
	 * driver.navigate().back() driver.navigate().forward()
	 * driver.navigate().refresh()
	 * 
	 * driver.getCurrentUrl()-->to get the current url of the webpage
	 * 
	 * driver.getTitle()---> to get the current page title
	 * 
	 * driver.close()---> to close the current window
	 * 
	 * driver.quit()---> to terminate the browsersession
	 * 
	 * 
	 */
}
